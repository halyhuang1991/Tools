export class Triangle { 
    constructor(public name: string) {
        this.name = name;
    }
    // 类的构造函数，只在实例化时被调用，而且被调用一次； 
    log() { 
        console.log(this.name); 
    }
 }
export class Square { 
    
}